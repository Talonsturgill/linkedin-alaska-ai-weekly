DRAFT NOT FIRED FROM THIS ROUTINE SESSION.

Subject: Alaska.Ai — Anchorage Desk Draft — 2026-05-20
To:      Talon.sturgill@gmail.com
Payload: out/gmail_payload.json   (subject + htmlBody, ready to POST)
Preview: out/gmail_html_body.html (open in a browser to review)

ROOT CAUSE (confirmed this session, matches prior diagnostic 1c5877d):

  Every call to the Gmail MCP from this Routine session returns
  "Streamable HTTP error: Error POSTing to endpoint: MCP tool call
  requires approval" — including tiny test payloads and read-only
  list_drafts calls.

  - .claude/settings.local.json correctly allowlists the friendly
    aliases mcp__Gmail__create_draft and mcp__Gmail__search_threads
    that ToolSearch surfaces in this session.
  - No UUID-prefixed canonical Gmail tool surfaces in ToolSearch
    from EITHER the orchestrator OR a spawned subagent.
  - The error originates at the MCP transport layer, not the
    harness allowlist (read-only list_drafts, which isn't in the
    allowlist, returns the SAME error).

  This is the same failure mode commit 1c5877d documented for the
  2026-05-20 Stack run. The fix that worked there was for the
  user to open a NEW non-Routine session — where the
  UUID-prefixed canonical Gmail name surfaced — and fire the
  draft from this exact payload file.

TO FIRE THE DRAFT NOW:

  1. Open a fresh, non-Routine Claude Code session in this repo
     (e.g. via the desktop app or web at claude.ai/code).
  2. Ask it to ToolSearch for Gmail tools. If a UUID-prefixed
     name appears, have it call THAT name with the contents of
     out/gmail_payload.json.
  3. Or paste out/gmail_html_body.html into a new Gmail compose
     window (rich text), set the subject above, save as draft.

I attempted six things in this session to fire the draft directly:
friendly alias with full payload, friendly alias with minimal
payload, friendly alias for list_drafts (allowlist-independent
test), search_threads, broad ToolSearch queries for any UUID-prefixed
Gmail tool, and a subagent retry. All six produced the identical
"requires approval" transport error. The blocker is the connector
wiring for THIS session type, not anything I can change from in
here without infra-level fixes.
